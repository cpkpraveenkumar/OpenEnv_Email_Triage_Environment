name: openenv-email-triage
version: 0.1.0
description: "OpenEnv compliant email triage environment with easy/medium/hard tasks."
author: "GitHub Copilot"
source: "https://github.com/example/openenv-email-triage"
api:
  - endpoint: "/reset"
  - endpoint: "/step"
  - endpoint: "/state"
  - endpoint: "/tasks"
  - endpoint: "/grader"
  - endpoint: "/baseline"
entrypoint: "app.main:app"
type: "python"
schema:
  observation: "env.schemas.Observation"
  action: "env.schemas.Action"
  reward: "env.schemas.Reward"
